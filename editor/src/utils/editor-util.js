//
function findNodeInConfigTree(dom) {

}

// add node to dom as children
function addChildNodeToConfigTree(component, dom) {
  const targetNode = findNodeInConfigTree(dom);
}

// append node to dom as brother
function appendNodeToConfigTree(component, dom) {
  const targetNode = findNodeInConfigTree(dom);
}

// prepend node to dom as brother
function prependNodeToConfigTree(component, dom) {
  const targetNode = findNodeInConfigTree(dom);
}

function getConfigTreeNodeById(id) {

}

// core
// render state tree to react
function renderConfigTree(config) {

}

export default {
  findNodeInConfigTree,
}
